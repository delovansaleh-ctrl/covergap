version: 1
frontend:
  phases:
    build:
      commands:
        - mkdir -p dist
        - cp index.html app.js dist/
        - cp -r fonts dist/
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths: []
